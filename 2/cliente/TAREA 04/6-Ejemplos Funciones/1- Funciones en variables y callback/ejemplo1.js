// Creamos función add
function add(paramOne, paraTwo) {
    return paramOne + paraTwo;
}


// Guardamos una llamada a la función add 
let resultA = add(2, 3);

// Mostramos el resultado guardado en la variable
console.log(resultA);


